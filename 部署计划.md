# 部署计划 — knowledge-web CI/CD 完整操作手册

> 目标：推代码到 GitHub → 自动构建 → 产出 Docker 镜像并推送到镜像仓库。
> **边界：到镜像为止，不包含任何部署动作。** 部署能力仅做预留设计，本期不实现。
> 文档版本：v3（2025-09-05）
> v3 定位：**手把手操作手册**——覆盖环境准备、本地配置文件逐个创建、本地验证每步预期输出、GitHub 网页逐步操作（建仓、SSH、Actions、Packages），以及日常使用循环与故障排查。
> 阅读方式：第 1~2 章是方案与原理（建议通读一遍）；第 3~6 章是按顺序执行的操作步骤（照着做）；第 7~12 章是日常使用与保障资料（用到再查）。

---

# 第 1 章 方案总览

## 1.1 目标与边界

```
git push（main 分支 或 v* 标签）
        │
        ▼
GitHub Actions 自动触发（约 2~4 分钟）
        │
   ① Checkout ──── 拉取代码
   ② Buildx ────── 准备构建器 + 加载缓存
   ③ Login ─────── 登录镜像仓库
   ④ Metadata ──── 计算本次镜像标签
   ⑤ Build&Push ── 执行 Dockerfile：
   │      ├─ npm ci            安装依赖
   │      ├─ sync-notes        重建软链
   │      ├─ check-md          门禁检查
   │      ├─ vitepress build   构建静态产物
   │      └─ 打入 nginx 镜像    形成最终镜像
   │
   ▼
✅ 镜像已推送到 GHCR，任意机器 docker pull 即可用
```

**本期不做**：任何自动部署（不 SSH、不 kubectl、不通知服务器）、多云推送（只推 GHCR）、smoke-test 进流水线（Playwright 较重，留作可选开关）。

**验收标准**：你以后要做的只有 `git push`，约 2~4 分钟后在 GHCR 看到新镜像。

## 1.2 关键选型一览

| 决策项 | 选择 | 一句话理由 |
|---|---|---|
| 镜像仓库 | **GHCR**（ghcr.io） | 用 Actions 自带 `GITHUB_TOKEN` 认证，零 Secrets、零第三方账号 |
| 镜像形态 | 多阶段：`node:22-alpine` 构建 → `nginx:1.27-alpine` 运行 | 最终镜像 ~25MB，不含 Node / node_modules / 源码 |
| 触发 | push main / `v*` 标签 / 手动按钮；PR 只验证不推送 | 日常 push 即出镜像，打 tag 即出版本 |
| 标签 | `main`、`latest`、`sha-xxx`、`1.2.3`、`1.2` | `latest` 跟随主干，版本标签永不变，`sha-*` 精确回滚 |
| 缓存 | Buildx `type=gha` 层缓存 | 依赖不变时 `npm ci` 直接命中，流水线缩短一半 |
| 门禁 | `check-md` 在镜像构建内部执行 | 坏笔记 → 构建失败 → 不推送，坏镜像出不去 |

> 为什么选 GHCR 而不是 Docker Hub / 阿里云 ACR：GHCR 与 GitHub 原生集成，推送认证用流水线内置令牌，**不需要注册任何第三方账号、不需要在仓库里配置任何 Secret**；镜像名自动跟随仓库名。切换到其他仓库只需改 workflow 3 行（见 §4.5 备选说明）。

## 1.3 本计划涉及的文件变更总览

| 文件 | 动作 | 作用 |
|---|---|---|
| `.gitignore` | 追加条目 | 排除调试截图等开发产物 |
| `Dockerfile` | 新增 | 定义镜像怎么构建（两阶段） |
| `.dockerignore` | 新增 | 挡住无关文件，缩小构建上下文 |
| `nginx.conf` | 新增 | 容器内 Nginx 的 gzip / 缓存 / 路由配置 |
| `.github/workflows/build-image.yml` | 新增 | 定义"什么时候自动构建、怎么推镜像" |
| `README.md` | 完善 | 面向公开仓库的门面文档（另行交付） |

其余项目文件**零改动**（`config.mjs`、`package.json`、笔记均不动）。

---

# 第 2 章 流水线逐步详解（原理篇）

本章把流水线从触发到产出**每一步**拆开，逐条说明：做什么、为什么需要、失败了会怎样。§2.2~2.7 对应 workflow 的 5 个 step，§2.8 对应 Dockerfile 内部逐步动作，§2.9 讲缓存，§2.10 讲最终产物。

## 2.1 全景时序与耗时预估

| # | 步骤 | 首次运行 | 缓存命中后 | 失败后果 |
|---|---|---|---|---|
| 0 | 触发判定（GitHub 侧） | 秒级 | 秒级 | 不触发即无事发生 |
| 1 | Checkout 拉代码 | ~5s | ~5s | 流水线终止 |
| 2 | Setup Buildx + 载入缓存 | ~10s | ~10s | 流水线终止 |
| 3 | Login GHCR | ~2s | ~2s | 流水线终止（权限问题） |
| 4 | Metadata 标签计算 | <1s | <1s | 流水线终止（配置错误） |
| 5 | Build & Push：npm ci | 60~90s | **0s（层缓存命中，跳过）** | 不推送，GHCR 保持旧镜像 |
| 6 | Build & Push：sync-notes + check-md | ~3s | ~3s | 同上 |
| 7 | Build & Push：vitepress build | 30~60s | 30~60s（内容变了必重跑） | 同上 |
| 8 | Build & Push：nginx 阶段组装镜像 | ~5s | ~5s | 同上 |
| 9 | 推送镜像层到 GHCR | 10~20s | 10~20s | 构建成功但不更新仓库 |
| | **合计** | **~4 min** | **~2 min** | |

> 关键安全性质：**任何一步失败，流水线立即终止且不推送**。GHCR 上永远是"最近一次成功构建"的镜像，坏镜像不可能覆盖好镜像。

## 2.2 触发阶段：GitHub 怎么决定跑不跑

workflow 文件头部的 `on:` 块就是触发规则，GitHub 对每个 push/PR 事件逐条比对：

| 事件 | 规则匹配 | 结果 |
|---|---|---|
| `git push origin main` | `branches: [main]` 命中 | 触发，且 `push=true` → 会推送镜像 |
| `git push origin v0.2.0` | `tags: ['v*']` 命中（v 开头） | 触发，metadata 额外生成版本标签 |
| push 其他分支（如 `dev`） | 不匹配 | **不触发**（省额度；想试可手动触发） |
| 开/更新 PR 指向 main | `pull_request` 命中 | 触发，但 `push=false` → 只构建验证 |
| Actions 页点 "Run workflow" | `workflow_dispatch` 命中 | 手动触发 |

**作用**：把"哪些变更值得产出镜像"变成声明式配置——日常只推 main，实验分支乱推也不烧构建时间。

## 2.3 Step 1 — Checkout（拉取代码）

```yaml
- name: Checkout
  uses: actions/checkout@v4
```

- **做什么**：把仓库代码完整检出到 GitHub 提供的 Ubuntu 虚拟机（runner）的工作目录。
- **为什么需要**：后续一切操作（Docker 构建）的对象就是这份代码；没有它一切无从谈起。
- **细节**：PR 触发时检出的是"PR 合并到 main 后的预览代码"，验证的是合并结果而非分支本身。
- **失败会怎样**：几乎不会失败；若仓库异常则流水线直接终止。

## 2.4 Step 2 — Setup Buildx（准备 Docker 构建器 + 缓存通道）

```yaml
- name: Setup Buildx
  uses: docker/setup-buildx-action@v3
```

- **做什么**：在 runner 上安装并启用 Docker Buildx 构建器。
- **为什么需要**：普通 `docker build` 不支持把构建层缓存上传到 GitHub 缓存服务。Buildx 配合 `cache-from/cache-to: type=gha` 才能实现"第二次构建跳过 npm ci"。**这是全流水线最重要的提速机制。**
- **失败会怎样**：罕见；失败则流水线终止。

## 2.5 Step 3 — Login to GHCR（登录镜像仓库）

```yaml
- name: Login to GHCR
  if: github.event_name != 'pull_request'
  uses: docker/login-action@v3
  with:
    registry: ghcr.io
    username: ${{ github.actor }}
    password: ${{ secrets.GITHUB_TOKEN }}
```

- **做什么**：向 `ghcr.io` 完成认证，为推送镜像取得凭证。
- **为什么需要**：推镜像必须登录。密码用 **`GITHUB_TOKEN`——Actions 自动注入的临时令牌**，配合文件头部 `permissions: packages: write` 即可推包，**全程无需手动配置任何 Secret**；令牌随流水线结束自动失效。
- **`if:` 条件的作用**：PR 事件跳过登录——PR 只构建不推送，无需凭证。
- **失败会怎样**：多为 workflow 的 `permissions` 块被改动所致，恢复即可（见 §9 排查表）。

## 2.6 Step 4 — Docker Metadata（计算本次镜像标签）

```yaml
- name: Docker metadata
  id: meta
  uses: docker/metadata-action@v5
  with:
    images: ghcr.io/${{ github.repository }}
    tags: |
      type=ref,event=branch
      type=ref,event=pr
      type=semver,pattern={{version}}
      type=semver,pattern={{major}}.{{minor}}
      type=sha,prefix=sha-
      type=raw,value=latest,enable={{is_default_branch}}
```

- **做什么**：根据本次触发事件，自动算出该打哪些镜像标签，输出给下一步引用。
- **为什么需要**：标签策略集中一处声明，避免手工拼字符串出错；还会自动把镜像名转小写（GHCR 要求小写，GitHub 用户名可能含大写）、自动附加 OCI 标准标注。
- **逐条解读**：

| 规则 | 含义 | 示例产出 |
|---|---|---|
| `type=ref,event=branch` | 分支名作标签 | `:main` |
| `type=ref,event=pr` | PR 编号作标签 | `:pr-12`（仅 PR 事件） |
| `type=semver,pattern={{version}}` | 完整语义化版本 | `v1.2.3` → `:1.2.3` |
| `type=semver,pattern={{major}}.{{minor}}` | 主.次版本 | `v1.2.3` → `:1.2`（自动跟进同系列补丁） |
| `type=sha,prefix=sha-` | 提交哈希短标签 | `:sha-abc1234`（唯一指纹，可精确回滚） |
| `type=raw,value=latest,enable={{is_default_branch}}` | 仅默认分支加 `latest` | `:latest` |

- **失败会怎样**：仅当 `tags:` 语法写错时失败。

## 2.7 Step 5 — Build and Push（构建并推送镜像）

```yaml
- name: Build and push
  uses: docker/build-push-action@v6
  with:
    context: .
    push: ${{ github.event_name != 'pull_request' }}
    tags: ${{ steps.meta.outputs.tags }}
    labels: ${{ steps.meta.outputs.labels }}
    cache-from: type=gha
    cache-to: type=gha,mode=max
```

- **做什么**：读取 `Dockerfile` 执行完整多阶段构建（内部动作见 §2.8），成功后推送到 GHCR。**流水线的执行核心**，前 4 步都为它做准备。
- **逐项参数作用**：

| 参数 | 作用 |
|---|---|
| `context: .` | 构建上下文 = 仓库根目录；`.dockerignore` 在此生效 |
| `push: …` | 动态决定：PR 时 `false` 只构建验证；push/手动时 `true` 真正推送 |
| `tags / labels` | 引用上一步 metadata 的计算结果 |
| `cache-from: type=gha` | 构建前从 GitHub 缓存**下载**上次的 Docker 层缓存 |
| `cache-to: type=gha,mode=max` | 构建后把**所有阶段**的层缓存上传（含依赖层），供下次复用 |

- **失败会怎样**：最可能失败的步骤（笔记语法、依赖问题），但失败即终止且不推送——**GHCR 旧镜像不受影响**。到 Actions 页看红色日志，对照 §9 排查。

## 2.8 Dockerfile 逐行详解（镜像内部实际发生的事）

#### Stage 1 — 构建阶段（临时施工现场，不进最终镜像）

```dockerfile
FROM node:22-alpine AS build
```
- **做什么**：以精简 Node 镜像为底，建立名为 `build` 的构建阶段。
- **为什么**：VitePress 需要 Node 跑构建；`alpine` 变体比完整镜像小一个数量级。此阶段只是"施工现场"。

```dockerfile
WORKDIR /app
```
- **做什么**：设定后续指令的工作目录为 `/app`。

```dockerfile
COPY package.json package-lock.json ./
RUN npm ci
```
- **做什么**：**只先拷贝两个依赖清单**，然后按 lock 文件精确安装依赖。
- **为什么先只拷清单**：镜像构建提速的**第一关键设计**。Docker 层缓存按"指令 + 输入"判定——lock 没变，即使改了 100 个笔记，这一层直接命中缓存，**60~90 秒的依赖安装变成 0 秒**。
- **`npm ci` vs `npm install`**：`ci` 严格按 lock 安装、更快、可复现、lock 与 package.json 不同步时直接报错——CI 的正确选择。

```dockerfile
COPY . .
RUN npm run sync-notes
```
- **做什么**：拷贝其余源码与笔记；重建 `docs/notes → ../notes` 软链。
- **为什么**：git 在 Linux 下保存软链没问题，但 Windows clone、压缩传输会破坏软链，构建时读不到笔记直接失败。`sync-notes` 先删旧链再重建，**幂等**，兜底所有环境差异，只花 1 秒。
- **为什么 `COPY . .` 不带垃圾**：`.dockerignore` 已挡掉 node_modules、构建产物、调试截图等，实际只拷源码、配置和笔记。

```dockerfile
RUN npm run check-md \
 && npm run build
```
- **做什么**：① 门禁——扫描笔记中代码块外的 `{{`/`{%`（会被 Vue 模板引擎当插值，炸构建），有错报到文件:行号；② 正式构建——产出静态文件到 `docs/.vitepress/dist/`。
- **为什么门禁放这里**：任一步失败 → 构建中止 → **不推送**。坏笔记永远出不了镜像。check-md 仅 ~2 秒，先跑能把错误定位从"构建堆栈"提前到"哪篇笔记哪一行"。

#### Stage 2 — 运行时阶段（最终镜像只有这里的内容）

```dockerfile
FROM nginx:1.27-alpine
```
- **做什么**：切换到**全新干净的** Nginx 基础镜像（~20MB）。
- **为什么**：多阶段构建精髓——Stage 1 的 Node、依赖、源码**一个字节都不进最终镜像**。

```dockerfile
COPY nginx.conf /etc/nginx/conf.d/default.conf
```
- **做什么**：写入站点配置。作用：① gzip 压缩文本资源（体积减 60%+）；② `/assets/` 带哈希资源一年强缓存；③ 页面协商缓存（更新即生效）；④ `try_files $uri $uri/ $uri.html =404` 兼容 VitePress 产物规则。

```dockerfile
COPY --from=build /app/docs/.vitepress/dist /usr/share/nginx/html
```
- **做什么**：**只**把静态产物从 Stage 1 拷进 Nginx 网页根目录。这一行是两阶段的"交接面"，交接的只有成品。

```dockerfile
EXPOSE 80
HEALTHCHECK --interval=30s --timeout=3s --start-period=5s \
  CMD wget -qO- http://127.0.0.1/ >/dev/null 2>&1 || exit 1
```
- **`EXPOSE 80`**：声明容器监听 80 端口（文档性声明 + `docker run -p` 提示）。
- **`HEALTHCHECK`**：每 30 秒自检本机 HTTP，连续失败标记 `unhealthy`，`docker ps` 可见、编排系统可自动重启。用 busybox 自带 `wget`，零额外安装。

## 2.9 缓存机制：为什么第二次快一倍

| 缓存 | 载体 | 命中条件 | 免掉的开销 |
|---|---|---|---|
| **Docker 层缓存**（主力） | `cache-from/to: type=gha` | 层的指令和输入未变 | `npm ci` 60~90s（lock 不变时） |
| **Buildx `mode=max`** | 同上 | — | 连 Stage 1 中间层也缓存 |
| **镜像层去重**（推送侧） | GHCR 按 blob 去重 | 层内容相同 | 推送只传变化层，10~20s |

| 你改了什么 | 命中情况 | 耗时 |
|---|---|---|
| 只改了笔记 | 依赖层命中；build 层重跑（必须） | ~2 min |
| 什么都没改（手动重跑） | 全部命中 | ~1 min |
| 升级了依赖（lock 变了） | `npm ci` 重装；其余照旧 | ~4 min |

> 注：流水线中所有 `npm` 操作都发生在 Docker 构建内部，因此 workflow 不需要 `actions/setup-node` 或 npm 缓存——依赖缓存完全由 Buildx gha 层缓存承担。

## 2.10 流水线结束时你得到什么

1. **GHCR 上的新镜像**，本次事件对应全部标签同时指向它。
2. **仓库右侧 Packages 入口**可浏览镜像与版本历史。
3. **Actions 页绿色构建记录**，`sha-*` 标签对应确切提交。
4. **任何机器立即可用**：
   ```bash
   docker pull ghcr.io/<owner>/knowledge-web:latest
   docker run -d -p 8080:80 ghcr.io/<owner>/knowledge-web:latest
   ```

---

# 第 3 章 阶段一：环境准备（本地 · 一次性）

## 3.1 Git：检查 → 安装 → 首次配置

```bash
# 检查是否已安装（本机已装 2.x 即可跳过安装）
git --version
# 预期输出示例：git version 2.43.0
```

若未安装：
- **Ubuntu/Debian**：`sudo apt update && sudo apt install -y git`
- **macOS**：`brew install git`（或 `xcode-select --install`）
- **Windows**：下载 [git-scm.com](https://git-scm.com/downloads) 安装包，一路默认

首次使用需声明身份（**这会写进每个提交的作者信息，GitHub 按此关联你的账号**）：

```bash
git config --global user.name "你的GitHub用户名"
git config --global user.email "你的GitHub邮箱"
# 建议同时把默认分支设为 main，与 workflow 的 branches: [main] 保持一致
git config --global init.defaultBranch main
```

> **注意**：`user.email` 最好用 GitHub 账号绑定的邮箱（可在 GitHub → Settings → Emails 查看），否则提交不会关联到你的头像和贡献图。

## 3.2 Node.js：检查

```bash
node -v    # 预期 ≥ 18（本机 v24.19.0）
npm -v     # 预期 ≥ 9
```

若版本过低，安装 LTS 版本：[nodejs.org](https://nodejs.org/) 或用 nvm 管理（`nvm install --lts`）。本地开发才需要 Node；CI 构建用的是 Docker 内自带的 Node，与你本机版本无关。

## 3.3 Docker：检查 → 安装 → 验证

```bash
docker --version
# 预期输出示例：Docker version 27.x.x
docker compose version   # 可选，第 8 章会用到
```

若未安装：
- **推荐 Desktop 版**（自带 compose）：[Docker Desktop](https://www.docker.com/products/docker-desktop/)（Windows/macOS）
- **Linux**：`curl -fsSL https://get.docker.com | sh` 然后把当前用户加入 docker 组：`sudo usermod -aG docker $USER`（**重新登录终端后生效**）

验证 Docker 能正常拉镜像和运行：

```bash
docker run --rm hello-world
# 预期输出：Hello from Docker! ……（一段说明文字）
```

> 拉取缓慢时可配置国内镜像加速器（Docker Desktop → Settings → Docker Engine → registry-mirrors，或 Linux 的 `/etc/docker/daemon.json`）。

## 3.4 配置 SSH key（让本机能免密推送到 GitHub）

```bash
# 1) 生成密钥对（邮箱换成你的；一路回车用默认路径即可）
ssh-keygen -t ed25519 -C "你的GitHub邮箱"
# 预期输出：Your identification has been saved in ~/.ssh/id_ed25519 ……

# 2) 查看并复制公钥内容（是 .pub 结尾的那个！）
cat ~/.ssh/id_ed25519.pub
# 预期输出：ssh-ed25519 AAAAC3Nza…… 你的邮箱
```

把公钥添加到 GitHub（网页操作）：

1. 登录 GitHub → 右上角头像 → **Settings**
2. 左侧栏最下方 → **SSH and GPG keys**
3. 点绿色按钮 **New SSH key**
4. Title 随便填（如 `我的开发机`），Key type 保持 `Authentication Key`，把上面复制的公钥**整行**粘进 Key 框
5. 点 **Add SSH key**（可能要求输入 GitHub 密码确认）

回到终端验证连通性：

```bash
ssh -T git@github.com
# 预期输出：Hi <用户名>! You've successfully authenticated, but GitHub does not provide shell access.
```

> **备选：HTTPS + Personal Access Token**（不方便配 SSH 时用）
> GitHub → Settings → Developer settings → Personal access tokens → **Tokens (classic)** → Generate new token (classic) → 勾选 `repo` 权限 → 生成后复制（只显示一次）。之后首次 `git push` 时：Username 填 GitHub 用户名，Password 粘贴这个 token。现代 git 会通过 credential helper 记住它。

---

# 第 4 章 阶段二：本地配置文件操作（逐个创建）

> **操作方式**：以下文件用任何编辑器创建均可（VS Code、vim 等）；也可以直接复制本章的终端命令块一次性创建。创建位置全部在**项目根目录** `/srv/knowledge-web/` 下。

## 4.0 先动 `.gitignore`（追加，不是替换）

现有 `.gitignore` 已排除 node_modules 等构建产物，**在文件末尾追加**以下内容（排除根目录 60+ 张调试截图与研究快照——它们与构建无关，入库只浪费空间）：

```gitignore
# 开发调试截图 / 研究快照（不入库）
dev-*.png
.home_*.png
research-*.html
research-data.json
.ms-playwright/
```

终端方式追加：

```bash
cat >> .gitignore << 'EOF'

# 开发调试截图 / 研究快照（不入库）
dev-*.png
.home_*.png
research-*.html
research-data.json
.ms-playwright/
EOF
```

## 4.1 创建 `Dockerfile`

位置：项目根目录，文件名就叫 `Dockerfile`（无扩展名）。

```dockerfile
# syntax=docker/dockerfile:1

# ========== 阶段 1：构建静态产物 ==========
FROM node:22-alpine AS build
WORKDIR /app

# 1) 先只拷贝依赖清单 —— 源码变动时命中缓存，跳过 npm ci
COPY package.json package-lock.json ./
RUN npm ci

# 2) 拷贝源码并重建 notes 软链（防 Windows/压缩传输导致软链失效）
COPY . .
RUN npm run sync-notes

# 3) 门禁检查 + 构建（任何一步失败则镜像构建失败，坏镜像出不去）
RUN npm run check-md \
 && npm run build

# ========== 阶段 2：Nginx 运行时 ==========
FROM nginx:1.27-alpine

COPY nginx.conf /etc/nginx/conf.d/default.conf
COPY --from=build /app/docs/.vitepress/dist /usr/share/nginx/html

EXPOSE 80

HEALTHCHECK --interval=30s --timeout=3s --start-period=5s \
  CMD wget -qO- http://127.0.0.1/ >/dev/null 2>&1 || exit 1
```

> 每一行为什么这么写，见 §2.8 逐行详解。唯一可调项：`node:22-alpine` 可换成 `node:24-alpine` 与本机对齐。

## 4.2 创建 `.dockerignore`

位置：项目根目录，必须与 Dockerfile 同级（Docker 读它时按 Dockerfile 所在目录找）。

```
# 依赖与缓存
node_modules/
.npm-cache/
docs/.vitepress/cache/
docs/.vitepress/dist/

# Git 与 CI（镜像内不需要）
.git/
.github/

# 开发调试产物
dev-*.png
.home_*.png
.ms-playwright/
research-*.html
research-data.json

# 文档与环境
*.log
.DS_Store
```

**作用**：`docker build` 会把上下文目录整体发给构建引擎，没有这个文件，几百 MB 的 node_modules 和截图都会被打包进去——构建慢且镜像里可能混入无关内容。

## 4.3 创建 `nginx.conf`

位置：项目根目录（Dockerfile 里按这个路径引用它，别放进子目录）。

```nginx
server {
    listen 80;
    server_name _;
    root  /usr/share/nginx/html;
    index index.html;

    # 文本资源 gzip（VitePress 产物以 html/js/css/svg 为主）
    gzip on;
    gzip_min_length 1024;
    gzip_types text/plain text/css application/javascript
               application/json image/svg+xml;

    # 带内容哈希的静态资源：一年强缓存
    location /assets/ {
        add_header Cache-Control "public, max-age=31536000, immutable";
    }

    # 页面与零散资源：协商缓存，更新即生效
    location / {
        # VitePress 产物规则：foo.md → foo.html，目录 → index.html
        try_files $uri $uri/ $uri.html =404;
        add_header Cache-Control "public, max-age=0, must-revalidate";
    }
}
```

## 4.4 创建 workflow 文件

**先创建目录**（GitHub 只认 `.github/workflows/` 这个确切路径）：

```bash
mkdir -p .github/workflows
```

再在该目录下创建 `build-image.yml`（完整路径 `.github/workflows/build-image.yml`）：

```yaml
name: build-image

on:
  push:
    branches: [main]
    tags: ['v*']
  pull_request:
    branches: [main]
  workflow_dispatch:

# 最小权限：只读代码 + 写镜像包
permissions:
  contents: read
  packages: write

jobs:
  build-and-push:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Buildx
        uses: docker/setup-buildx-action@v3

      # PR 时只构建不推送，跳过登录
      - name: Login to GHCR
        if: github.event_name != 'pull_request'
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Docker metadata
        id: meta
        uses: docker/metadata-action@v5
        with:
          images: ghcr.io/${{ github.repository }}
          tags: |
            type=ref,event=branch
            type=ref,event=pr
            type=semver,pattern={{version}}
            type=semver,pattern={{major}}.{{minor}}
            type=sha,prefix=sha-
            type=raw,value=latest,enable={{is_default_branch}}

      - name: Build and push
        uses: docker/build-push-action@v6
        with:
          context: .
          push: ${{ github.event_name != 'pull_request' }}
          tags: ${{ steps.meta.outputs.tags }}
          labels: ${{ steps.meta.outputs.labels }}
          cache-from: type=gha
          cache-to: type=gha,mode=max
```

**易错点提醒**：
- YAML 对缩进敏感：`steps:` 下的每一项用 2 空格缩进对齐，**不要用 Tab**。
- `permissions:` 块是 GHCR 推送权限的来源，删掉它 Login/Push 步骤必挂。
- 文件名随意（这里是 `build-image.yml`），但**必须在 `.github/workflows/` 下且后缀 `.yml`/`.yaml`**。

> **备选：推送到 Docker Hub / 阿里云 ACR**
> ① 仓库 Settings → Secrets and variables → Actions → New repository secret，添加 token（如 `DOCKERHUB_USERNAME` / `DOCKERHUB_TOKEN`）；
> ② Login 步骤改为 `username: ${{ secrets.DOCKERHUB_USERNAME }}` / `password: ${{ secrets.DOCKERHUB_TOKEN }}`，删去 `registry:` 行；
> ③ `images:` 改为 `docker.io/<用户名>/knowledge-web`。其余不变。

## 4.5 创建后自查

```bash
ls -la Dockerfile .dockerignore nginx.conf .github/workflows/build-image.yml
git status --short
# 预期看到（M = 修改，?? = 新增）：
#  M .gitignore
# ?? .dockerignore
# ?? Dockerfile
# ?? .github/
# ?? nginx.conf
```

五个文件都在、`git status` 能看到它们，本章完成。**先不要 commit**，做完第 5 章本地验证再提交。

---

# 第 5 章 阶段三：本地验证（每步命令 + 预期输出）

目的：在推上 GitHub 之前把整条链路本地走通，保证首次 CI 一次通过。逐条执行：

**① 模拟 CI 依赖安装**

```bash
npm ci
# 预期：添加 xxx 个包，无 error 字样
# 失败说明：package-lock.json 与 package.json 脱节 → 本地 npm install 修复后把 lock 一起提交
```

**② 重建软链**

```bash
npm run sync-notes
# 预期输出：✓ symlinked docs/notes -> ../notes
```

**③ 门禁预检**

```bash
npm run check-md
# 预期输出：✅ 笔记检查通过：没有代码块外的 {{ 或 {%
# 失败会精确列出 文件:行号 → 把 {{ 换成 &#123;&#123;、{% 换成 &#123;% 后重跑
```

**④ 本地构建**

```bash
npm run build
# 预期：vitepress v1.6.4 开始构建，输出 docs/.vitepress/dist 目录，无报错退出
# 这步不过，CI 必挂——先在本地修好
```

**⑤ 构建 Docker 镜像**（与 CI 中 Build & Push 做的事完全相同）

```bash
docker build -t knowledge-web:local .
# 预期：依次执行 Stage 1（npm ci → sync-notes → check-md → build）与 Stage 2（nginx），
#       最后输出 => => naming to docker.io/library/knowledge-web:local
# 首次需下载 node:22-alpine 与 nginx:1.27-alpine 基础镜像，耐心等几分钟
```

**⑥ 启动容器**

```bash
docker run -d --name kw-test -p 8080:80 knowledge-web:local
# 预期输出一长串容器 ID；-d 后台运行，-p 8080:80 把容器 80 端口映射到本机 8080
docker ps
# 预期：STATUS 列显示 Up x seconds，稍后应显示 (healthy) —— 说明 HEALTHCHECK 生效
```

**⑦ 验证服务**

```bash
curl -I http://localhost:8080/
# 预期：HTTP/1.1 200 OK
```

再用浏览器打开 `http://localhost:8080`，逐项确认：首页正常、笔记列表能打开、暗色主题切换正常、搜索能用。

**⑧ 清理**

```bash
docker rm -f kw-test
```

八步全绿 → 进入第 6 章推上 GitHub。

---

# 第 6 章 阶段四：GitHub 远程仓库操作（网页逐步）

## 6.1 登录 GitHub

浏览器打开 [github.com](https://github.com) 并登录。没有账号则 Sign up 注册（免费）。

## 6.2 创建空仓库（每一步点击）

1. 右上角 **+** 号 → **New repository**（或直接访问 `github.com/new`）
2. **Repository name**：填 `knowledge-web`（镜像名会自动带上它）
3. **Description**（可选）：如 `个人 Markdown 笔记站 · VitePress · Docker 镜像交付`
4. **可见性二选一**（⚠️ 关键决定）：
   - **Public**：任何人可见你的笔记内容、可匿名拉镜像
   - **Private**：只有你能看；镜像包默认也 private，拉取需 `docker login ghcr.io`
   - > 笔记涉密务必选 Private。选错了也能在 Settings 里随时改。
5. **不要勾选** "Add a README file"、"Add .gitignore"、"Choose a license"——本地已有完整项目，勾了会产生与本地无共同祖先的提交，首推冲突
6. 点绿色按钮 **Create repository**

创建后 GitHub 会显示一页 Quick setup 提示（给出 remote 地址），留着别关，下一步要用其中的仓库地址。

## 6.3 关联远端并首推（终端操作）

```bash
# 1) 初始化本地仓库（主分支直接叫 main，与 workflow 匹配）
git init -b main
# 预期输出：Initialized empty Git repository in /srv/knowledge-web/.git/

# 2) 暂存全部文件（.gitignore/.dockerignore 会自动挡掉垃圾文件）
git add -A

# 3) 提交
git commit -m "chore: init repo + ci pipeline"
# 预期输出：一长串 create mode ... 清单，最后一行如 main (root-commit) abc1234

# 4) 关联远端（把 <你的用户名> 换掉；SSH 方式）
git remote add origin git@github.com:<你的用户名>/knowledge-web.git

# 5) 首次推送（-u 建立跟踪关系，以后裸敲 git push 即可）
git push -u origin main
# 预期输出：Enumerating objects ... 、分支设置成功字样
# ★ 这次 push 本身满足触发条件，流水线此刻已经自动开跑了
```

> 用 HTTPS 方式的，第 4 步地址换成 `https://github.com/<你的用户名>/knowledge-web.git`，推送时按提示输入用户名和 token（见 §3.4 备选）。

## 6.4 观察 Actions 首跑（网页操作）

1. 仓库页面顶部标签栏 → **Actions**
2. 左侧列表应有 **build-image** 工作流，右侧是运行记录，最新一条的标题就是你的提交信息
3. 状态图标：🟡 黄圈 = 进行中；✅ 绿勾 = 成功；❌ 红叉 = 失败
4. 点进任意一条运行记录可看到 5 个 step 逐个展开；绿色的 step 可折叠，**失败时点红的那一步**看日志最后几十行即可定位（对照第 9 章排查表）
5. 首次无缓存约 4 分钟；完成后该条记录变 ✅

## 6.5 查看镜像包

两个入口任选：
- 仓库首页右侧栏 → **Packages** 区块
- 或个人主页 → **Packages** 标签

应看到 `knowledge-web` 包，点进去有版本列表、每个 tag 和对应的拉取命令 `docker pull ghcr.io/<你的用户名>/knowledge-web:main`。

## 6.6 设置包可见性为 Public（可选 · 一次性）

> 作用：改完后任何机器**不需要 docker login** 就能拉镜像。保持 private 也完全可用（拉取前登录即可）。

1. GitHub 右上角头像 → **Your packages**
2. 点 **knowledge-web** 包
3. 右下角 **Package settings**
4. 拉到页面底部 **Danger Zone** → **Change visibility** → 选 **Public**
5. 按提示输入包名确认 → 确认按钮

> 之后每次推送新版本**自动保持** Public，无需重复设置。包与仓库的关联由 metadata-action 自动写入的 source 标注完成，无需手动 link。

## 6.7 仓库设置建议（可选）

| 设置 | 路径 | 建议 |
|---|---|---|
| Actions 权限确认 | Settings → Actions → General → Workflow permissions | 保持默认 "Read and write permissions" 或显式 permissive；我们 workflow 内已声明 `packages: write`，通常无需改动 |
| 仓库简介与话题 | 首页右上 About ⚙️ | 填 Description、加 topics（`vitepress`、`markdown`、`docker`）利于检索 |
| 分支保护 | Settings → Branches → Add rule | 可选：对 `main` 开 "Require a pull request before merging"（单人项目可不开） |

---

# 第 7 章 日常使用循环（三场景）

初始化完成后，日常只有三种场景：

### 场景 A：更新笔记或代码（最常用）

| 步骤 | 操作 | 作用 |
|---|---|---|
| 1 | 在 `notes/`（或 `docs/`）里写/改 `.md` | 内容变更——别在代码块外写裸 `{{`/`{%`，门禁会拦 |
| 2 | `git add -A && git commit -m "说明"` | 固化为提交。提交是 CI 追踪单位，`sha-*` 标签来自它 |
| 3 | `git push` | 推到 main → **自动触发流水线**。你唯一要做的动作，到此收工 |
| 4 | （可看可不看）Actions 页 | 自动完成门禁 → 构建 → 推镜像，约 2~4 分钟 |
| 5 | 任何机器 `docker pull ghcr.io/<owner>/knowledge-web:latest` | `latest` 已指向最新成功构建 |

### 场景 B：发布正式版本

| 步骤 | 操作 | 作用 |
|---|---|---|
| 1 | 确认 main 最近一次构建是绿色 | 发版基于已验证代码 |
| 2 | `git tag v0.2.0 && git push origin v0.2.0` | 推标签 → 流水线产出 `:0.2.0`、`:0.2` 镜像 |
| 3 | 稳定优先时拉 `:0.2.0` | 版本标签永不变；`latest` 继续跟随 main |

### 场景 C：试验性改动（PR）与手动触发

| 步骤 | 操作 | 作用 |
|---|---|---|
| 1 | 开分支提交 → 向 main 发 PR | 流水线只构建验证、**不推送镜像**——验证改动能否构建出合格镜像 |
| 2 | 看 PR 上的 check 状态 | 绿 = 可放心合并；红 = 先修再合 |
| 3 | （可选）Actions → build-image → Run workflow | 手动触发一次完整构建推送，适合"重出镜像" |

---

# 第 8 章 镜像的常见使用方式（速查）

```bash
# 拉取与运行
docker pull ghcr.io/<owner>/knowledge-web:latest
docker run -d --name knowledge-web -p 8080:80 ghcr.io/<owner>/knowledge-web:latest

# 状态与健康检查
docker ps                     # STATUS 列看 Up / (healthy)
docker logs knowledge-web     # 看 Nginx 访问/错误日志
docker stop knowledge-web && docker rm knowledge-web   # 停止并删除

# 更新到最新镜像
docker pull ghcr.io/<owner>/knowledge-web:latest
docker rm -f knowledge-web
docker run -d --name knowledge-web -p 8080:80 ghcr.io/<owner>/knowledge-web:latest
```

`docker-compose.yml` 示例（长期跑在某台服务器/NAS 上时更省心）：

```yaml
services:
  knowledge-web:
    image: ghcr.io/<owner>/knowledge-web:latest
    container_name: knowledge-web
    ports:
      - "8080:80"
    restart: unless-stopped
```

之后更新只需：`docker compose pull && docker compose up -d`。

> 从源码本地构建镜像：`docker build -t knowledge-web .`（不经过 GitHub，离线/调试用）。

---

# 第 9 章 失败排查对照表

| 失败步骤 | 典型报错 | 原因 | 处理 |
|---|---|---|---|
| `ssh -T git@github.com`（§3.4） | `Permission denied (publickey)` | 公钥没加对/没加 | 确认粘贴的是 `.pub` 文件内容；`ssh-add` 或重新走 §3.4 |
| `git push` | `Repository not found` | remote 地址拼错或无权限 | `git remote -v` 核对 URL；确认仓库名、用户名正确 |
| `git push` 被拒绝 | `fetch first` / `rejected` | 远端有本地没有的提交（多为误勾了初始化 README） | `git pull --rebase origin main` 后再推；或确认后 `git push -f`（仅限刚建仓无人协作时） |
| Login GHCR | `denied` / `resource not accessible` | workflow 的 `permissions` 缺 `packages: write` | 恢复 §4.4 的 permissions 块 |
| Docker build: `npm ci` | `lock file … not in sync` | 改过 `package.json` 没更新 lock | 本地 `npm install` 后把 `package-lock.json` 一起提交 |
| Docker build: `check-md` | `❌ notes/xxx.md:12: {{ …` | 笔记代码块外有裸插值 | 按行号转义：`&#123;&#123;` / `&#123;%` |
| Docker build: `build` | VitePress 报错堆栈 | Markdown 语法/主题组件问题 | 本地 `npm run build` 复现修复 |
| Push 镜像 | `denied: …` | 组织/仓库包权限限制（个人仓库罕见） | Settings → Actions → Workflow permissions 检查 |
| GHCR 找不到包 | Packages 页为空 | 首次成功构建前包不存在 | 构建绿色后刷新；个人页 Packages 查看 |
| 全程没触发 | push 后 Actions 无记录 | 分支不是 `main`，或 workflow 文件没推上去 | `git branch --show-current` 核对；确认 `.github/workflows/build-image.yml` 在仓库里 |
| 触发了但秒挂 | workflow file issue 类 | yaml 语法错误（常见：Tab 缩进） | 用 §4.4 原文整体覆盖 |

---

# 第 10 章 实施清单

| # | 事项 | 章节 | 状态 |
|---|---|---|---|
| 1 | Git 安装 + 全局配置（name/email/默认分支） | §3.1 | ☐ |
| 2 | Node 检查（≥18） | §3.2 | ☐ |
| 3 | Docker 安装 + `hello-world` 验证 | §3.3 | ☐ |
| 4 | SSH key 生成、添加到 GitHub、`ssh -T` 验证 | §3.4 | ☐ |
| 5 | `.gitignore` 追加条目 | §4.0 | ☐ |
| 6 | 创建 `Dockerfile` | §4.1 | ☐ |
| 7 | 创建 `.dockerignore` | §4.2 | ☐ |
| 8 | 创建 `nginx.conf` | §4.3 | ☐ |
| 9 | `mkdir -p .github/workflows` + 创建 `build-image.yml` | §4.4 | ☐ |
| 10 | `git status` 自查五个文件 | §4.5 | ☐ |
| 11 | 本地八步验证全绿 | §5 | ☐ |
| 12 | GitHub 创建空仓库（不勾初始化文件） | §6.2 | ☐ |
| 13 | `git init` + commit + remote + 首推 | §6.3 | ☐ |
| 14 | Actions 首跑 ✅、Packages 出现镜像 | §6.4~6.5 | ☐ |
| 15 | （可选）包可见性改 Public | §6.6 | ☐ |

---

# 第 11 章 后续扩展预留（本期不实现）

未来需要"推镜像 = 自动部署"时，只在现有 workflow 追加一个 job，前 5 步不动：

```yaml
  # 预留示例：SSH 到服务器 docker compose pull && up -d
  # deploy:
  #   needs: build-and-push
  #   if: github.ref == 'refs/heads/main'
  #   runs-on: ubuntu-latest
  #   steps:
  #     - name: Deploy over SSH
  #       uses: appleboy/ssh-action@v1
  #       with:
  #         host: ${{ secrets.SSH_HOST }}
  #         username: ${{ secrets.SSH_USER }}
  #         key: ${{ secrets.SSH_KEY }}
  #         script: cd /opt/knowledge-web && docker compose pull && docker compose up -d
```

其他方向：构建成功通知（钉钉/飞书/Telegram）、smoke-test 门禁、多架构镜像（`linux/arm64`，树莓派/NAS）。

---

# 第 12 章 风险与注意事项

| 风险 | 说明 | 对策 |
|---|---|---|
| 软链失效 | `docs/notes` 软链在 Windows/tar 传输下会丢 | Dockerfile 内 `npm run sync-notes` 强制重建（§2.8） |
| `{{` 插值炸构建 | 笔记正文裸 `{{` 会让构建失败 | `check-md` 门禁，失败不推送（§2.8） |
| 坏镜像覆盖好镜像 | — | **结构性不可能**：任何步骤失败即终止且不推送 |
| 公开仓库与笔记隐私 | **Public 仓库 = 笔记对全世界可见** | 涉密内容用 Private 仓库；或公开仓库中只放可公开的笔记。提交前自查 `git status` |
| Playwright 体积 | `smoke-test` 依赖浏览器二进制（数百 MB） | 本期不进流水线；未来加入需 `npx playwright install --with-deps chromium` |
| `latest` 漂移 | `latest` 永远在变 | 固定版本用 `v*` / `sha-*` 标签；`latest` 仅表"最新可用" |
| Actions 免费额度 | 公共仓库无限免费；私有仓库每月 2000 分钟（免费档） | 单次流水线 2~4 分钟，个人使用远在额度内 |
| gha 缓存膨胀 | 缓存累积占存储（单仓库约 10 GB 上限，LRU 自动淘汰） | 无需干预；介意可 Settings 手动清除 |
